import type { Project } from "./schema";

/**
 * BOS - bilerek.
 *
 * Football Squad Optimizer'in gercek verileri architecture.md §3'te hazir, ama
 * semanin zorunlu tuttugu `screenshot` icin gercek bir dosya henuz yok.
 * Placeholder gorsel konmaz, alan da `.optional()` yapilmaz (CLAUDE.md kural 6
 * ve 7). Kayit, gercek ekran goruntusu uretildiginde Faz 3'te eklenir.
 */
export const projects: Project[] = [];
